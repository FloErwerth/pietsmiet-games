{
  "version": "1.0.0",
  "workspaces": ["backend","app"],
  "scripts": {
    "run": "run-p run:backend run:app",
    "run:backend": "npm run startBackend -w backend",
    "run:app": "npm run startBackend -w app"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
